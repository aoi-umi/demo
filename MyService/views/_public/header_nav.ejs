<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="javascript:;"><%= siteName %></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <%
                var list = [{
                    text: 'Index',
                    url: '/'
                }];
                list.forEach(function(t, i){
                %>
                <li>
                    <a class="" href="<%= t.url || 'javascript:;' %>"><%= t.text %></a>
                </li>
                <%
                });
                %>
            </ul>
            <ul class="nav navbar-nav navbar-right nav-sign <%= user.id ? 'in' : '' %>">
                <li class="sign in">
                    <a class="nav-user-link" href="/user/<%= user.id %>">
                        您好，
                        <span class="nav-nickname">
                            <%= user.nickname %>
                        </span>
                    </a>
                </li>
                <li class="sign in">
                    <a class="sign-out" href="javascript:;">注销</a>
                </li>
                <li class="sign out"><a href="/sign/up">注册</a></li>
                <li class="sign out"><a class="sign-in" href="javascript:;">登录</a></li>
            </ul>
        </div>
    </div>
</nav>

<!--登录-->
<div id="signInBox" data-backdrop="false" role="dialog" tabindex="-1" class="modal fade" style="z-index: 1040">
    <div class="modal-dialog" style="top:100px">
        <div class="modal-content">
            <div class="modal-body">
                <button class="close" type="button" data-dismiss="modal">
                    ×
                </button>
                <h4 name="title" class="modal-title">
                    登录
                </h4>
            </div>
            <div class="modal-body">
                <form class="form-horizontal sign-in-form">
                    <div class="form-group">
                        <label class="col-sm-2 control-label">用户名</label>
                        <div class="col-sm-10">
                            <input class="form-control sign-in-input" name="account" placeholder="请输用户名" value="">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label">密码</label>
                        <div class="col-sm-10">
                            <input class="form-control sign-in-input" name="password" placeholder="请输用密码" value=""
                                   type="password">
                        </div>
                    </div>
                    <button type="button" class="btn btn-default sign-in">登录</button>
                </form>
            </div>
        </div>
    </div>
</div>