<tbody class="itemRow">
<tr>
    <td><%= account %>(<%= nickname || '' %>#<%= id %>)</td>
    <td><% authority_list.forEach(function (t) { %>
        <div class="label <%= t.status == 1 ? 'label-info' : 'label-default' %> my-label">
            <label><%= t.code %></label>
        </div>
        <% }); %></td>
    <td><%= create_datetime %></td>
    <td><%= edit_datetime || '' %></td>
    <td><%= remark || '' %></td>
    <td>
        <a class="itemDetailQuery" href="javascript:;">详细</a>
    </td>
</tr>
</tbody>